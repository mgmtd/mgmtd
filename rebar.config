{erl_opts, [debug_info]}.
{deps, [
  %% OTP 27+ provides json in stdlib; this is dropped at compile time there.
  {json_polyfill, "~> 0.2"}
]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [mgmtd]}
]}.
